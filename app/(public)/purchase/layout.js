

export const metadata = {
    title: "Crown Purchase",
    description: "The FineOps software for make business finance effectively",
};

export default function DashboardLayout({ children }) {
    return <section>{children}</section>
}